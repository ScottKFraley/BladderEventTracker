import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class AuthService {
  // NEED to implement methods for:
  // Login: Send login credentials to your backend API, store authentication tokens (e.g., JWT) in local storage or session storage.
  // //Register: Send registration data to your backend API.
  // Logout: Clear authentication tokens and user data from storage.
  // Check Authentication Status: Check if a user is authenticated based on the presence of tokens.


  constructor() { }
}
